import { useMemo, useState } from "react";
import type { ReactNode } from "react";
import {
  Activity, ArrowRight, ArrowUpRight, Bell, CalendarDays, Check, ChevronDown, CircleHelp,
  ClipboardList, CloudSun, Command, CreditCard, FileText, Globe2, Headphones, LayoutDashboard,
  LifeBuoy, Menu, Moon, Plane, Plus, Search, Settings2, ShieldCheck, Sun, Ticket, Users, WalletCards, X,
} from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Separator } from "@/components/ui/separator";
import { toast } from "sonner";
import { demoCredentials, seedBookings, seedIncidents, seedRoutes, seedUsers, type DemoBooking, type DemoUser, type Role } from "@/lib/demoData";

// Mila: a frontend-only airline booking prototype. Demo JSON seed data is intentionally local;
// authentication, persistence, and enforcement should move to a backend before production use.

type Language = "en" | "am";
type AuthMode = "signin" | "register";
type Modal = "booking" | "user" | "route" | "profile" | null;

const roleLabels: Record<Role, Record<Language, string>> = {
  super: { en: "Super Admin", am: "ሱፐር አስተዳዳሪ" },
  technical: { en: "Technical Team", am: "የቴክኒክ ቡድን" },
  client: { en: "Client", am: "ደንበኛ" },
};

const roleDescription: Record<Role, Record<Language, string>> = {
  super: { en: "Control users, revenue, routes, and every booking workspace.", am: "ተጠቃሚዎችን፣ ገቢን፣ መንገዶችን እና ቦታ ማስያዣዎችን ይቆጣጠሩ።" },
  technical: { en: "Monitor integrations, service health, and incidents across the network.", am: "ውህደቶችን፣ የሲስተም ጤናን እና ችግሮችን ይከታተሉ።" },
  client: { en: "Create bookings, track passengers, and keep every trip moving.", am: "ቦታ ማስያዣ ይፍጠሩ፣ ተሳፋሪዎችን ይከታተሉ።" },
};

const text = {
  en: { dashboard: "Dashboard", bookings: "Bookings", routes: "Routes & schedules", customers: "Customers", support: "Support center", team: "Team & permissions", settings: "Workspace settings", overview: "Overview", current: "Aug 01 — Aug 31, 2026", newBooking: "New booking", recent: "Recent bookings", users: "User management", addUser: "Add user", addRoute: "Add route", status: "Status", search: "Search bookings, routes, clients", signin: "Sign in", register: "Create demo account", signout: "Sign out", firstName: "Full name", email: "Email address", password: "Password", chooseRole: "Choose a role", continue: "Continue to Mila", demo: "Demo access", useDemo: "Use demo credentials", welcome: "Keep every movement visible.", subtitle: "A calm booking control surface for airlines, agencies, and travel teams.", explore: "Explore the workspace", seeRoles: "See role access", featureOne: "One workspace", featureTwo: "Three clear roles", featureThree: "Ready for any booking app", governance: "Governance", systems: "System health", clientDesk: "Client desk", empty: "No results found" },
  am: { dashboard: "ዳሽቦርድ", bookings: "ቦታ ማስያዣዎች", routes: "መንገዶች እና የጊዜ ሰሌዳ", customers: "ደንበኞች", support: "የድጋፍ ማዕከል", team: "ቡድን እና ፈቃዶች", settings: "ቅንብሮች", overview: "አጠቃላይ እይታ", current: "ነሐሴ 01 — ነሐሴ 31፣ 2018", newBooking: "አዲስ ቦታ ማስያዣ", recent: "የቅርብ ጊዜ ማስያዣዎች", users: "የተጠቃሚ አስተዳደር", addUser: "ተጠቃሚ ጨምር", addRoute: "መንገድ ጨምር", status: "ሁኔታ", search: "ማስያዣ፣ መንገድ ወይም ደንበኛ ይፈልጉ", signin: "ግባ", register: "የሙከራ መለያ ፍጠር", signout: "ውጣ", firstName: "ሙሉ ስም", email: "ኢሜይል", password: "የይለፍ ቃል", chooseRole: "ሚና ይምረጡ", continue: "ወደ Mila ይቀጥሉ", demo: "የሙከራ መዳረሻ", useDemo: "የሙከራ መረጃ ተጠቀም", welcome: "እያንዳንዱን እንቅስቃሴ ይመልከቱ።", subtitle: "ለአየር መንገዶች፣ ኤጀንሲዎች እና የጉዞ ቡድኖች የተዘጋጀ የማስያዣ መቆጣጠሪያ።", explore: "የስራ ቦታን ይመልከቱ", seeRoles: "ሚናዎችን ይመልከቱ", featureOne: "አንድ የስራ ቦታ", featureTwo: "ሶስት ግልጽ ሚናዎች", featureThree: "ለማንኛውም የማስያዣ መተግበሪያ", governance: "አስተዳደር", systems: "የሲስተም ጤና", clientDesk: "የደንበኛ ዴስክ", empty: "ምንም ውጤት የለም" },
};

const navBase = [
  { key: "dashboard", icon: LayoutDashboard },
  { key: "bookings", icon: ClipboardList },
  { key: "routes", icon: Plane },
  { key: "customers", icon: Users },
  { key: "support", icon: LifeBuoy },
] as const;

export default function Home() {
  const [language, setLanguage] = useState<Language>("en");
  const [dark, setDark] = useState(false);
  const [session, setSession] = useState<{ name: string; email: string; role: Role } | null>(null);
  const [authOpen, setAuthOpen] = useState(false);
  const [authMode, setAuthMode] = useState<AuthMode>("signin");
  const [authRole, setAuthRole] = useState<Role>("client");
  const [modal, setModal] = useState<Modal>(null);
  const [mobileOpen, setMobileOpen] = useState(false);
  const [activeNav, setActiveNav] = useState("dashboard");
  const [search, setSearch] = useState("");
  const [users, setUsers] = useState<DemoUser[]>(seedUsers);
  const [bookings, setBookings] = useState<DemoBooking[]>(seedBookings);
  const [authName, setAuthName] = useState("");
  const [authEmail, setAuthEmail] = useState("");
  const [authPassword, setAuthPassword] = useState("");
  const [formName, setFormName] = useState("");
  const [formEmail, setFormEmail] = useState("");
  const [formRoute, setFormRoute] = useState("ADD → DXB");
  const [formPassenger, setFormPassenger] = useState("");
  const t = text[language];
  const role = session?.role ?? "client";

  const filteredBookings = useMemo(() => bookings.filter((b) => `${b.id} ${b.passenger} ${b.route}`.toLowerCase().includes(search.toLowerCase())), [bookings, search]);
  const notify = (message: string) => toast(message);
  const toggleTheme = () => setDark((current) => { const next = !current; document.documentElement.classList.toggle("dark", next); return next; });

  const openAuth = (mode: AuthMode, selectedRole: Role = "client") => { setAuthMode(mode); setAuthRole(selectedRole); setAuthOpen(true); };
  const signInDemo = (selectedRole: Role) => { const credential = demoCredentials.find((item) => item.role === selectedRole)!; setAuthEmail(credential.email); setAuthPassword(credential.password); setAuthRole(selectedRole); setAuthMode("signin"); };
  const submitAuth = () => {
    if (!authEmail || !authPassword || (authMode === "register" && !authName)) { notify(language === "en" ? "Complete the required fields first" : "እባክዎ አስፈላጊ መስኮችን ይሙሉ"); return; }
    const matching = demoCredentials.find((item) => item.email === authEmail && item.password === authPassword);
    const chosenRole = matching?.role ?? authRole;
    setSession({ name: authName || (chosenRole === "super" ? "Mila Admin" : chosenRole === "technical" ? "Mila Technical" : "Mila Client"), email: authEmail, role: chosenRole });
    setAuthOpen(false); notify(language === "en" ? `Signed in as ${roleLabels[chosenRole].en}` : `እንደ ${roleLabels[chosenRole].am} ገብተዋል`);
  };
  const switchRole = (nextRole: Role) => { if (!session) return; setSession({ ...session, role: nextRole }); notify(`${roleLabels[nextRole][language]} workspace selected`); };
  const submitBooking = () => { if (!formPassenger) { notify("Add a passenger name first"); return; } const next: DemoBooking = { id: `ML-${20850 + bookings.length}`, passenger: formPassenger, route: formRoute, departure: "Aug 18 · 08:30", status: "Pending", total: "$420.00" }; setBookings([next, ...bookings]); setFormPassenger(""); setModal(null); notify("Booking created in the demo ledger"); };
  const submitUser = () => { if (!formName || !formEmail) { notify("Add a name and email first"); return; } const initials = formName.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase(); setUsers([...users, { id: `USR-${String(users.length + 1).padStart(3, "0")}`, name: formName, email: formEmail, role: authRole, status: "Pending", initials }]); setFormName(""); setFormEmail(""); setModal(null); notify("User added to the demo directory"); };

  if (!session) return <><Landing language={language} dark={dark} t={t} toggleTheme={toggleTheme} setLanguage={setLanguage} openAuth={openAuth} />{authOpen && <AuthDialog mode={authMode} role={authRole} language={language} name={authName} email={authEmail} password={authPassword} setName={setAuthName} setEmail={setAuthEmail} setPassword={setAuthPassword} setRole={setAuthRole} onModeChange={setAuthMode} onClose={() => setAuthOpen(false)} onSubmit={submitAuth} onDemo={signInDemo} />}</>;

  return <div className="min-h-screen bg-background text-foreground">
    <aside className={`fixed inset-y-0 left-0 z-40 flex w-[272px] flex-col border-r border-sidebar-border bg-sidebar px-5 py-6 transition-transform duration-300 lg:translate-x-0 ${mobileOpen ? "translate-x-0" : "-translate-x-full"}`}>
      <div className="flex items-center justify-between px-2"><Brand onClick={() => setActiveNav("dashboard")} /><button className="rounded-lg p-2 text-muted-foreground hover:bg-sidebar-accent lg:hidden" onClick={() => setMobileOpen(false)}><X size={18} /></button></div>
      <div className="mt-10 px-2"><span className="wayfinding-label text-muted-foreground">MILA · {roleLabels[role][language]}</span></div>
      <nav className="mt-3 space-y-1">{navBase.map(({ key, icon: Icon }) => { const active = activeNav === key; return <button key={key} onClick={() => { setActiveNav(key); setMobileOpen(false); }} className={`nav-item flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-[13px] font-semibold transition-all ${active ? "bg-sidebar-accent text-sidebar-accent-foreground shadow-sm" : "text-muted-foreground hover:bg-sidebar-accent/70 hover:text-sidebar-accent-foreground"}`}><Icon size={17} /><span>{key === "dashboard" ? t.dashboard : key === "bookings" ? t.bookings : key === "routes" ? t.routes : key === "customers" ? t.customers : t.support}</span>{active && <span className="ml-auto h-2 w-2 rounded-full bg-[#F26A3D]" />}</button>; })}</nav>
      <div className="mt-8 px-2"><span className="wayfinding-label text-muted-foreground">{role === "super" ? t.governance : role === "technical" ? t.systems : t.clientDesk}</span></div>
      <nav className="mt-3 space-y-1">{(role === "super" ? [{ icon: ShieldCheck, label: t.users, action: () => setModal("user") }, { icon: Settings2, label: t.settings, action: () => notify("Workspace settings opened") }] : role === "technical" ? [{ icon: Activity, label: "Service health", action: () => setActiveNav("support") }, { icon: FileText, label: "Incidents", action: () => notify("Incident queue opened") }] : [{ icon: Ticket, label: "My trips", action: () => setActiveNav("bookings") }, { icon: CircleHelp, label: t.support, action: () => notify("Support center opened") }]).map(({ icon: Icon, label, action }) => <button key={label} onClick={action} className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-[13px] font-semibold text-muted-foreground transition hover:bg-sidebar-accent hover:text-sidebar-accent-foreground"><Icon size={17} /><span>{label}</span></button>)}</nav>
      <div className="mt-auto rounded-2xl border border-sidebar-border bg-sidebar-accent/60 p-4"><div className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-emerald-500" /><span className="wayfinding-label text-muted-foreground">SYSTEM STATUS</span></div><p className="mt-3 text-[13px] font-semibold">All systems operational</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Demo data synced 2 minutes ago</p></div>
    </aside>
    {mobileOpen && <button className="fixed inset-0 z-30 bg-black/20 backdrop-blur-sm lg:hidden" onClick={() => setMobileOpen(false)} />}
    <main className="min-h-screen lg:pl-[272px]">
      <header className="sticky top-0 z-20 border-b border-border/70 bg-background/90 backdrop-blur-xl"><div className="flex h-[72px] items-center gap-4 px-5 sm:px-8"><button className="rounded-lg p-2 text-muted-foreground hover:bg-accent lg:hidden" onClick={() => setMobileOpen(true)}><Menu size={20} /></button><div className="relative hidden max-w-[360px] flex-1 sm:block"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" size={16} /><Input value={search} onChange={(e) => setSearch(e.target.value)} placeholder={t.search} className="h-10 rounded-xl border-border/70 bg-card pl-9 text-xs shadow-none" /><span className="absolute right-3 top-1/2 hidden -translate-y-1/2 items-center gap-1 rounded-md border border-border px-1.5 py-0.5 text-[10px] text-muted-foreground md:flex"><Command size={10} /> K</span></div><div className="ml-auto flex items-center gap-2 sm:gap-3"><Button variant="outline" size="sm" className="hidden h-10 rounded-xl bg-card sm:flex" onClick={() => setLanguage(language === "en" ? "am" : "en")}><Globe2 size={15} className="mr-2" />{language === "en" ? "አማ" : "EN"}</Button><button onClick={toggleTheme} className="flex h-10 w-10 items-center justify-center rounded-xl border border-border/80 bg-card text-muted-foreground">{dark ? <Sun size={16} /> : <Moon size={16} />}</button><button onClick={() => notify("You have 3 demo notifications")} className="relative flex h-10 w-10 items-center justify-center rounded-xl border border-border/80 bg-card text-muted-foreground"><Bell size={16} /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-[#F26A3D]" /></button><div className="hidden h-8 w-px bg-border sm:block" /><button className="flex items-center gap-2" onClick={() => setModal("profile")}><div className="flex h-9 w-9 items-center justify-center rounded-full bg-[#183B56] text-xs font-bold text-white">{session.name.split(" ").map((p) => p[0]).join("").slice(0, 2)}</div><div className="hidden text-left leading-tight lg:block"><div className="text-xs font-bold">{session.name}</div><div className="text-[11px] text-muted-foreground">{roleLabels[role][language]}</div></div><ChevronDown size={14} className="text-muted-foreground" /></button></div></div></header>
      <div className="mx-auto max-w-[1480px] px-5 py-7 sm:px-8 sm:py-9"><section className="mb-7 flex flex-col justify-between gap-5 xl:flex-row xl:items-end"><div><div className="mb-3 flex items-center gap-2"><span className="h-px w-7 bg-[#F26A3D]" /><span className="wayfinding-label text-[#F26A3D]">{t.overview}</span></div><h1 className="font-display text-3xl font-bold tracking-[-0.045em]">{role === "super" ? "Good morning, Selam" : role === "technical" ? "Keep the network healthy" : "Your next movement starts here"}</h1><p className="mt-2 text-sm text-muted-foreground">{roleDescription[role][language]} <span className="mx-2 text-border">/</span> {t.current}</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" className="h-10 rounded-xl bg-card text-xs font-semibold" onClick={() => notify("Date range picker opened")}><CalendarDays size={15} className="mr-2" />{t.current}<ChevronDown size={14} className="ml-2" /></Button><Button className="h-10 rounded-xl bg-[#F26A3D] px-4 text-xs font-bold text-white" onClick={() => setModal("booking")}><Plus size={16} className="mr-2" />{t.newBooking}</Button></div></section>
        <RoleSwitcher role={role} language={language} onChange={switchRole} />
        {activeNav === "dashboard" && (<><div className="mb-7">{role === "super" && <SuperAdminPanel users={users} language={language} onAdd={() => setModal("user")} onNotify={notify} />}{role === "technical" && <TechnicalPanel language={language} onNotify={notify} />}{role === "client" && <ClientPanel language={language} onBook={() => setModal("booking")} />}</div><section className="mb-7 grid gap-4 md:grid-cols-2 xl:grid-cols-4"><StatCard label="Active bookings" value={role === "client" ? "4" : "1,284"} change="12.6%" icon={ClipboardList} /><StatCard label="Gross revenue" value={role === "client" ? "$1,925" : "$284.6k"} change="8.4%" icon={CreditCard} /><StatCard label="Passengers served" value={role === "technical" ? "3,842" : role === "client" ? "8" : "3,842"} change="4.2%" icon={Users} /><StatCard label="On-time departure" value="92.8%" change="1.8%" icon={Activity} negative={role === "technical"} /></section></>)}
        {activeNav === "bookings" && <BookingsPanel bookings={filteredBookings} onBook={() => setModal("booking")} onNotify={notify} />}
        {activeNav === "routes" && <RoutesPanel onAdd={() => setModal("route")} onNotify={notify} />}
        {activeNav === "customers" && <CustomersPanel users={users} onAdd={() => setModal("user")} onNotify={notify} />}
        {activeNav === "support" && <SupportPanel onNotify={notify} />}
        <section className={`mb-7 grid gap-5 xl:grid-cols-[minmax(0,1.35fr)_minmax(320px,0.65fr)] ${activeNav !== "dashboard" ? "hidden" : ""}`}><div className="dashboard-card min-h-[318px] overflow-hidden p-5 sm:p-6"><div className="flex items-start justify-between border-b border-dashed border-border/80 pb-4"><div><div className="flex items-center gap-2"><span className="waypoint-dot" /><div className="wayfinding-label text-muted-foreground">{role === "technical" ? "Service activity" : "Performance"}</div></div><h2 className="mt-2 font-display text-lg font-bold">{role === "technical" ? "Network events & uptime" : "Bookings & revenue trend"}</h2></div><button className="rounded-lg p-2 text-muted-foreground hover:bg-accent" onClick={() => notify("Performance view expanded")}><ArrowUpRight size={17} /></button></div><div className="mt-7 flex items-end justify-between gap-4"><div><div className="font-display text-[32px] font-bold tracking-[-0.05em]">{role === "technical" ? "99.96%" : "$284,620"}</div><div className="mt-1 flex items-center gap-2 text-xs text-muted-foreground"><span className="flex items-center gap-1 font-bold text-emerald-600"><ArrowUpRight size={13} /> 8.4%</span> from the previous period</div></div><div className="hidden items-center gap-4 text-[11px] text-muted-foreground sm:flex"><span className="flex items-center gap-1.5"><i className="h-2 w-2 rounded-full bg-[#F26A3D]" />{role === "technical" ? "Uptime" : "Revenue"}</span><span className="flex items-center gap-1.5"><i className="h-2 w-2 rounded-full bg-[#183B56]" />{role === "technical" ? "Events" : "Bookings"}</span></div></div><div className="mt-7 flex h-[120px] items-end gap-2 border-b border-border/80 sm:gap-3">{[46,64,56,78,62,84,72,92,74,88,80,96].map((height, index) => <div key={index} className="group relative flex h-full flex-1 items-end gap-1"><div className="w-full rounded-t-md bg-[#183B56]/75" style={{ height: `${Math.max(22, height - 12)}%` }} /><div className="w-full rounded-t-md bg-[#F26A3D]/80" style={{ height: `${height}%` }} /></div>)}</div><div className="mt-3 flex justify-between text-[10px] font-semibold uppercase tracking-[0.12em] text-muted-foreground"><span>Aug 01</span><span>Aug 08</span><span>Aug 15</span><span>Aug 22</span><span>Aug 31</span></div></div><div className="dashboard-card relative min-h-[318px] overflow-hidden bg-[#183B56] p-6 text-white"><div className="absolute inset-0 opacity-30" style={{ backgroundImage: "url('/manus-storage/ethiopian-route-map_2566119d.png')", backgroundPosition: "center", backgroundSize: "cover" }} /><div className="relative z-10 flex h-full flex-col"><div className="flex items-center justify-between border-b border-dashed border-white/20 pb-4"><div><div className="flex items-center gap-2"><span className="waypoint-dot waypoint-dot--light" /><div className="wayfinding-label text-white/55">{role === "technical" ? "Integration pulse" : "Next movement"}</div></div><h2 className="mt-2 font-display text-lg font-bold">ET 602 · Addis Ababa</h2></div><span className="rounded-full border border-white/20 bg-white/10 px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.12em] text-white/80">On time</span></div><div className="my-auto py-6"><div className="flex items-center gap-3"><div><div className="font-display text-3xl font-bold">ADD</div><div className="mt-1 text-xs text-white/55">06:40 EAT</div></div><div className="relative mx-1 h-px flex-1 bg-white/25"><span className="absolute left-1/2 top-1/2 flex h-7 w-7 -translate-x-1/2 -translate-y-1/2 items-center justify-center rounded-full border border-[#F26A3D]/50 bg-[#183B56]"><Plane size={13} className="rotate-90 text-[#F26A3D]" /></span></div><div className="text-right"><div className="font-display text-3xl font-bold">DXB</div><div className="mt-1 text-xs text-white/55">11:15 GST</div></div></div></div><div className="flex items-center justify-between border-t border-white/15 pt-4 text-xs"><span className="text-white/60">Gate B12 · 4h 35m · 238 seats</span><button onClick={() => notify("Manifest opened")} className="flex items-center gap-1.5 font-bold text-[#ff9b78]">View manifest <ArrowUpRight size={14} /></button></div></div></div></section>
        <section className="grid gap-5 xl:grid-cols-[minmax(0,1.35fr)_minmax(320px,0.65fr)]"><div className="dashboard-card overflow-hidden"><div className="flex flex-col gap-4 border-b border-border/70 p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><div><div className="wayfinding-label text-muted-foreground">{t.recent}</div><h2 className="mt-2 font-display text-lg font-bold">{role === "client" ? "My travel ledger" : "Booking manifest"}</h2></div><button onClick={() => setActiveNav("bookings")} className="flex items-center gap-1.5 text-xs font-bold text-[#F26A3D]">View all <ArrowUpRight size={14} /></button></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left"><thead className="bg-muted/35 text-[10px] uppercase tracking-[0.12em] text-muted-foreground"><tr><th className="px-5 py-3 font-bold sm:px-6">ID</th><th className="px-3 py-3 font-bold">Passenger</th><th className="px-3 py-3 font-bold">Route</th><th className="px-3 py-3 font-bold">Departure</th><th className="px-3 py-3 font-bold">{t.status}</th><th className="px-5 py-3 text-right font-bold sm:px-6">Total</th></tr></thead><tbody className="divide-y divide-border/70">{filteredBookings.length ? filteredBookings.map((booking) => <tr key={booking.id} className="transition hover:bg-muted/30"><td className="px-5 py-4 font-mono text-xs font-semibold text-muted-foreground sm:px-6">{booking.id}</td><td className="px-3 py-4 text-xs font-bold">{booking.passenger}</td><td className="px-3 py-4"><span className="route-chip"><span className="waypoint-dot waypoint-dot--small" />{booking.route}</span></td><td className="whitespace-nowrap px-3 py-4 text-xs text-muted-foreground">{booking.departure}</td><td className="px-3 py-4"><Badge variant="outline" className={`rounded-full border-0 px-2.5 py-1 text-[10px] font-bold ${booking.status === "Confirmed" ? "bg-emerald-100 text-emerald-700" : booking.status === "Pending" ? "bg-amber-100 text-amber-700" : "bg-rose-100 text-rose-700"}`}>{booking.status}</Badge></td><td className="px-5 py-4 text-right font-display text-xs font-bold sm:px-6">{booking.total}</td></tr>) : <tr><td colSpan={6} className="px-5 py-12 text-center text-sm text-muted-foreground">{t.empty}</td></tr>}</tbody></table></div></div><div className="space-y-5"><div className="dashboard-card p-5 sm:p-6"><div className="flex items-center justify-between"><div><div className="wayfinding-label text-muted-foreground">{role === "technical" ? "Open incidents" : "Flight health"}</div><h2 className="mt-2 font-display text-lg font-bold">{role === "technical" ? "Technical queue" : "Network snapshot"}</h2></div><Activity size={18} className="text-[#F26A3D]" /></div><div className="mt-5 space-y-4">{role === "technical" ? seedIncidents.map((incident) => <div key={incident.id} className="border-b border-dashed border-border pb-3 last:border-0"><div className="flex items-center justify-between gap-3 text-xs font-bold"><span>{incident.title}</span><Badge variant="outline" className="rounded-full border-0 bg-amber-100 px-2 py-1 text-[10px] text-amber-700">{incident.severity}</Badge></div><div className="mt-1 text-[11px] text-muted-foreground">{incident.owner} · {incident.updated}</div></div>) : <><HealthRow label="Flights today" value="38 / 42" percent={90} color="orange" /><HealthRow label="Seats occupied" value="78%" percent={78} color="blue" /><HealthRow label="Payment success" value="96.4%" percent={96} color="green" /></>}</div></div><div className="dashboard-card relative overflow-hidden p-5 sm:p-6"><div className="absolute right-0 top-0 h-24 w-24 rounded-bl-full bg-[#F26A3D]/10" /><div className="relative flex items-start justify-between"><div><div className="wayfinding-label text-muted-foreground">Addis Ababa weather</div><h2 className="mt-2 font-display text-lg font-bold">Bole International</h2><p className="mt-2 text-xs text-muted-foreground">Clear skies · 21°C</p></div><CloudSun size={25} className="text-[#F26A3D]" /></div><div className="mt-5 flex items-center justify-between"><div className="font-display text-4xl font-bold tracking-[-0.06em]">21°</div><div className="text-right text-[11px] leading-5 text-muted-foreground">Visibility 10km<br />Wind 8 km/h</div></div></div></div>        </section>
        <footer className="mt-8 flex flex-col justify-between gap-3 border-t border-border/70 pt-5 text-[11px] text-muted-foreground sm:flex-row"><div className="flex items-center gap-2"><span className="h-1.5 w-1.5 rounded-full bg-emerald-500" /> Mila Airline CMS · Demo mode</div><div className="flex flex-wrap items-center gap-4"><button onClick={() => notify("Support center opened")}>{t.support}</button><button onClick={() => notify("Workspace settings opened")}>{t.settings}</button><button onClick={() => { setSession(null); notify("Signed out of demo mode"); }}>{t.signout}</button></div></footer>
      </div>
    </main>
    {modal === "booking" && <Dialog title={t.newBooking} onClose={() => setModal(null)}><div className="space-y-4"><Field label="Passenger name" value={formPassenger} onChange={setFormPassenger} placeholder="e.g. Hana Alemu" /><label className="block text-xs font-bold">Route<select value={formRoute} onChange={(e) => setFormRoute(e.target.value)} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 text-sm"><option>ADD → DXB</option><option>ADD → LHR</option><option>ADD → NBO</option><option>CAI → ADD</option></select></label><Button className="h-11 w-full rounded-xl bg-[#F26A3D] text-white" onClick={submitBooking}>Create booking <ArrowRight size={16} className="ml-2" /></Button></div></Dialog>}
    {modal === "user" && <Dialog title={t.addUser} onClose={() => setModal(null)}><div className="space-y-4"><Field label={t.firstName} value={formName} onChange={setFormName} placeholder="e.g. Hana Alemu" /><Field label={t.email} value={formEmail} onChange={setFormEmail} placeholder="hana@example.com" /><label className="block text-xs font-bold">{t.chooseRole}<select value={authRole} onChange={(e) => setAuthRole(e.target.value as Role)} className="mt-2 h-10 w-full rounded-xl border border-border bg-background px-3 text-sm">{(Object.keys(roleLabels) as Role[]).map((item) => <option key={item} value={item}>{roleLabels[item][language]}</option>)}</select></label><Button className="h-11 w-full rounded-xl bg-[#F26A3D] text-white" onClick={submitUser}>Add user <Plus size={16} className="ml-2" /></Button><div className="max-h-48 space-y-2 overflow-y-auto border-t border-dashed border-border pt-4">{users.map((user) => <div key={user.id} className="flex items-center justify-between rounded-xl bg-muted/40 p-3"><div><div className="text-xs font-bold">{user.name}</div><div className="text-[11px] text-muted-foreground">{user.email}</div></div><Badge variant="outline" className="rounded-full border-0 bg-background text-[10px]">{roleLabels[user.role][language]}</Badge></div>)}</div></div></Dialog>}
    {modal === "route" && <Dialog title={t.addRoute} onClose={() => setModal(null)}><div className="space-y-3"><p className="text-sm text-muted-foreground">Create a route template for the demo schedule.</p><Field label="Route code" value="ML 220" onChange={() => undefined} placeholder="ML 220" /><Button className="h-11 w-full rounded-xl bg-[#F26A3D] text-white" onClick={() => { setModal(null); notify("Route added to demo schedule"); }}>Save route</Button></div></Dialog>}
    {modal === "profile" && <Dialog title="Demo profile" onClose={() => setModal(null)}><div className="space-y-4"><div className="rounded-2xl bg-muted/50 p-4"><div className="font-display text-lg font-bold">{session.name}</div><div className="mt-1 text-sm text-muted-foreground">{session.email}</div><Badge className="mt-3 rounded-full bg-[#F26A3D] text-white">{roleLabels[role][language]}</Badge></div><Button variant="outline" className="h-11 w-full rounded-xl" onClick={() => { setModal(null); setSession(null); }}>Sign out of demo</Button></div></Dialog>}
    {authOpen && <AuthDialog mode={authMode} role={authRole} language={language} name={authName} email={authEmail} password={authPassword} setName={setAuthName} setEmail={setAuthEmail} setPassword={setAuthPassword} setRole={setAuthRole} onModeChange={(nextMode) => { setAuthMode(nextMode); }} onClose={() => setAuthOpen(false)} onSubmit={submitAuth} onDemo={signInDemo} />}
  </div>;
}

function Landing({ language, dark, t, toggleTheme, setLanguage, openAuth }: { language: Language; dark: boolean; t: typeof text.en; toggleTheme: () => void; setLanguage: (value: Language) => void; openAuth: (mode: AuthMode, role?: Role) => void }) {
  return <div className="min-h-screen overflow-hidden bg-background text-foreground"><header className="relative z-10 flex items-center justify-between px-5 py-5 sm:px-10"><Brand onClick={() => undefined} /><div className="flex items-center gap-2"><button onClick={() => setLanguage(language === "en" ? "am" : "en")} className="flex h-10 items-center gap-2 rounded-xl border border-border bg-card px-3 text-xs font-bold"><Globe2 size={14} />{language === "en" ? "አማ" : "EN"}</button><button onClick={toggleTheme} className="flex h-10 w-10 items-center justify-center rounded-xl border border-border bg-card text-muted-foreground">{dark ? <Sun size={16} /> : <Moon size={16} />}</button><Button variant="outline" className="hidden h-10 rounded-xl bg-card sm:flex" onClick={() => openAuth("signin")}>{t.signin}</Button><Button className="h-10 rounded-xl bg-[#F26A3D] text-white" onClick={() => openAuth("register")}>{t.register}</Button></div></header><main className="relative mx-auto max-w-[1320px] px-5 pb-20 pt-8 sm:px-10 sm:pt-16"><div className="pointer-events-none absolute -right-24 -top-16 h-[520px] w-[520px] rounded-full bg-[#F26A3D]/10 blur-3xl" /><section className="grid items-center gap-12 lg:grid-cols-[1.1fr_0.9fr]"><div className="relative z-10 max-w-2xl"><div className="mb-5 flex items-center gap-2"><span className="h-px w-9 bg-[#F26A3D]" /><span className="wayfinding-label text-[#F26A3D]">THE BOOKING OPERATING SYSTEM</span></div><h1 className="max-w-3xl font-display text-5xl font-bold leading-[0.98] tracking-[-0.065em] sm:text-7xl">{t.welcome}</h1><p className="mt-6 max-w-xl text-base leading-7 text-muted-foreground sm:text-lg">{t.subtitle}</p><div className="mt-8 flex flex-wrap gap-3"><Button className="h-12 rounded-xl bg-[#F26A3D] px-5 text-white shadow-[0_12px_24px_rgba(242,106,61,0.22)]" onClick={() => openAuth("register")}>{t.explore}<ArrowRight size={17} className="ml-2" /></Button><Button variant="outline" className="h-12 rounded-xl bg-card px-5" onClick={() => document.getElementById("roles")?.scrollIntoView({ behavior: "smooth" })}>{t.seeRoles}</Button></div><div className="mt-10 grid max-w-lg grid-cols-3 border-y border-border py-5"><FeatureStat number="01" label={t.featureOne} /><FeatureStat number="03" label={t.featureTwo} /><FeatureStat number="∞" label={t.featureThree} /></div></div><div className="relative min-h-[420px] overflow-hidden rounded-[28px] bg-[#183B56] p-6 text-white shadow-[0_24px_60px_rgba(24,59,86,0.18)] sm:p-8"><div className="absolute inset-0 opacity-45" style={{ backgroundImage: "url('/manus-storage/airport-terminal-hero_a5b65f07.jpg')", backgroundPosition: "center", backgroundSize: "cover" }} /><div className="absolute inset-0 bg-gradient-to-br from-[#183B56]/90 via-[#183B56]/40 to-[#F26A3D]/40" /><div className="relative z-10 flex h-full flex-col justify-between"><div className="flex items-center justify-between"><span className="wayfinding-label text-white/55">MILA · LIVE DEMO</span><span className="flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-3 py-1.5 text-[10px] font-bold"><span className="h-1.5 w-1.5 rounded-full bg-emerald-400" /> OPERATIONAL</span></div><div><div className="wayfinding-label text-white/55">NEXT MOVEMENT</div><div className="mt-3 flex items-end justify-between"><div><div className="font-display text-5xl font-bold tracking-[-0.06em]">ADD</div><div className="mt-1 text-xs text-white/55">06:40 EAT · GATE B12</div></div><div className="mb-3 flex h-10 w-10 items-center justify-center rounded-full border border-[#F26A3D] bg-[#183B56]"><Plane size={17} className="rotate-90 text-[#F26A3D]" /></div><div className="text-right"><div className="font-display text-5xl font-bold tracking-[-0.06em]">DXB</div><div className="mt-1 text-xs text-white/55">11:15 GST</div></div></div><div className="mt-8 border-t border-dashed border-white/25 pt-4 text-xs text-white/65">One calm surface for bookings, routes, users, and service health.</div></div></div></div></section><section id="roles" className="mt-24"><div className="mb-8 flex items-end justify-between"><div><div className="wayfinding-label text-[#F26A3D]">ACCESS MAP</div><h2 className="mt-3 font-display text-3xl font-bold tracking-[-0.04em]">A clear desk for every team.</h2></div><span className="hidden text-sm text-muted-foreground sm:block">Choose a demo role to explore the workspace.</span></div><div className="grid gap-4 md:grid-cols-3">{(Object.keys(roleLabels) as Role[]).map((role, index) => <button key={role} onClick={() => openAuth("signin", role)} className="dashboard-card group relative overflow-hidden p-6 text-left transition duration-200 hover:-translate-y-1 hover:border-[#F26A3D]/50"><div className="flex items-center justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#F26A3D]/10 text-[#F26A3D]"><span className="font-display font-bold">0{index + 1}</span></span><ArrowUpRight size={17} className="text-muted-foreground transition group-hover:-translate-y-1 group-hover:translate-x-1 group-hover:text-[#F26A3D]" /></div><h3 className="mt-8 font-display text-xl font-bold">{roleLabels[role][language]}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{roleDescription[role][language]}</p></button>)}</div></section></main><footer className="border-t border-border px-5 py-6 text-center text-xs text-muted-foreground sm:px-10">Mila · Reusable airline booking CMS · Demo mode</footer></div>;
}

function AuthDialog({ mode, role, language, name, email, password, setName, setEmail, setPassword, setRole, onModeChange, onClose, onSubmit, onDemo }: { mode: AuthMode; role: Role; language: Language; name: string; email: string; password: string; setName: (v: string) => void; setEmail: (v: string) => void; setPassword: (v: string) => void; setRole: (v: Role) => void; onModeChange: (v: AuthMode) => void; onClose: () => void; onSubmit: () => void; onDemo: (v: Role) => void }) {
  const t = text[language];
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#10283a]/45 p-4 backdrop-blur-sm"><div className="relative w-full max-w-[480px] overflow-hidden rounded-[24px] border border-border bg-card shadow-[0_24px_70px_rgba(16,40,58,0.26)]"><button onClick={onClose} className="absolute right-4 top-4 rounded-lg p-2 text-muted-foreground hover:bg-accent"><X size={18} /></button><div className="p-6 sm:p-8"><div className="flex items-center gap-3"><span className="brand-mark flex h-10 w-10 items-center justify-center rounded-[14px] bg-[#F26A3D] text-white"><Plane size={18} className="rotate-90" /></span><div><div className="font-display text-lg font-bold">Mila</div><div className="wayfinding-label text-muted-foreground">{t.demo}</div></div></div><div className="mt-7 flex rounded-xl bg-muted/60 p-1"><button onClick={() => onModeChange("signin")} className={`flex-1 rounded-lg py-2 text-xs font-bold ${mode === "signin" ? "bg-card shadow-sm" : "text-muted-foreground"}`}>{t.signin}</button><button onClick={() => onModeChange("register")} className={`flex-1 rounded-lg py-2 text-xs font-bold ${mode === "register" ? "bg-card shadow-sm" : "text-muted-foreground"}`}>{t.register}</button></div><div className="mt-6 space-y-4">{mode === "register" && <Field label={t.firstName} value={name} onChange={setName} placeholder="Mekdes Tadesse" />}<Field label={t.email} value={email} onChange={setEmail} placeholder="client@mila.demo" type="email" /><Field label={t.password} value={password} onChange={setPassword} placeholder="mila123" type="password" /><div><div className="mb-2 text-xs font-bold">{t.chooseRole}</div><div className="grid grid-cols-3 gap-2">{(Object.keys(roleLabels) as Role[]).map((item) => <button key={item} onClick={() => setRole(item)} className={`rounded-xl border p-3 text-left text-[11px] font-bold transition ${role === item ? "border-[#F26A3D] bg-[#F26A3D]/10 text-[#D8532D]" : "border-border text-muted-foreground hover:border-[#F26A3D]/40"}`}><span className="mb-2 block h-2 w-2 rounded-full bg-[#F26A3D]" />{roleLabels[item][language]}</button>)}</div></div><Button className="h-11 w-full rounded-xl bg-[#F26A3D] text-white" onClick={onSubmit}>{mode === "signin" ? t.continue : t.register}<ArrowRight size={16} className="ml-2" /></Button><Separator /><div className="text-center"><div className="wayfinding-label text-muted-foreground">{t.useDemo}</div><div className="mt-2 flex justify-center gap-2">{(Object.keys(roleLabels) as Role[]).map((item) => <button key={item} onClick={() => onDemo(item)} className="rounded-lg border border-border px-2.5 py-2 text-[10px] font-bold text-muted-foreground hover:border-[#F26A3D]/40 hover:text-foreground">{roleLabels[item][language]}</button>)}</div><p className="mt-3 text-[11px] text-muted-foreground">Demo password: <span className="font-mono font-bold">mila123</span></p></div></div></div></div></div>;
}

function RoleSwitcher({ role, language, onChange }: { role: Role; language: Language; onChange: (role: Role) => void }) { return <section className="mb-7 overflow-hidden rounded-2xl border border-border/75 bg-card shadow-[0_8px_26px_rgba(24,59,86,0.04)]"><div className="flex flex-col gap-4 border-b border-dashed border-border/80 p-4 sm:flex-row sm:items-center sm:justify-between sm:p-5"><div><div className="flex items-center gap-2"><span className="waypoint-dot" /><span className="wayfinding-label text-muted-foreground">ROLE ACCESS</span></div><h2 className="mt-2 font-display text-base font-bold">{roleLabels[role][language]} workspace</h2><p className="mt-1 text-xs text-muted-foreground">{roleDescription[role][language]}</p></div><span className="rounded-full border border-[#F26A3D]/20 bg-[#F26A3D]/10 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.1em] text-[#D8532D]">Demo role control</span></div><div className="flex flex-wrap gap-2 p-4 sm:p-5">{(Object.keys(roleLabels) as Role[]).map((item) => <button key={item} onClick={() => onChange(item)} className={`flex items-center gap-2 rounded-xl border px-3 py-2 text-xs font-bold transition ${role === item ? "border-[#F26A3D] bg-[#F26A3D] text-white" : "border-border bg-background text-muted-foreground hover:border-[#F26A3D]/50"}`}><span className={`h-2 w-2 rounded-full ${role === item ? "bg-white" : "bg-[#F26A3D]"}`} />{roleLabels[item][language]}</button>)}</div></section>; }

function SuperAdminPanel({ users, language, onAdd, onNotify }: { users: DemoUser[]; language: Language; onAdd: () => void; onNotify: (message: string) => void }) { return <section className="mb-7 grid gap-5 lg:grid-cols-[1.25fr_0.75fr]"><div className="dashboard-card p-5 sm:p-6"><div className="flex items-center justify-between"><div><div className="wayfinding-label text-muted-foreground">ADMIN CONSOLE</div><h2 className="mt-2 font-display text-lg font-bold">User management</h2></div><Button size="sm" className="rounded-xl bg-[#F26A3D] text-white" onClick={onAdd}><Plus size={14} className="mr-1" /> Add user</Button></div><div className="mt-5 grid gap-2">{users.slice(0, 3).map((user) => <div key={user.id} className="flex items-center justify-between rounded-xl border border-border/70 bg-background/60 p-3"><div className="flex items-center gap-3"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-[#183B56] text-[10px] font-bold text-white">{user.initials}</span><div><div className="text-xs font-bold">{user.name}</div><div className="text-[11px] text-muted-foreground">{user.email}</div></div></div><div className="flex items-center gap-2"><Badge variant="outline" className="rounded-full border-0 bg-muted text-[10px]">{roleLabels[user.role][language]}</Badge><button onClick={() => onNotify(`${user.name} profile opened`)} className="rounded-lg p-2 text-muted-foreground hover:bg-accent"><ArrowUpRight size={14} /></button></div></div>)}</div></div><div className="dashboard-card p-5 sm:p-6"><div className="wayfinding-label text-muted-foreground">CONTROL TOWER</div><h2 className="mt-2 font-display text-lg font-bold">Workspace governance</h2><div className="mt-5 space-y-3"><GovernanceRow label="Active users" value={`${users.filter((u) => u.status === "Active").length}`} /><GovernanceRow label="Pending invites" value={`${users.filter((u) => u.status === "Pending").length}`} /><GovernanceRow label="Roles configured" value="03" /></div></div></section>; }

function TechnicalPanel({ language, onNotify }: { language: Language; onNotify: (message: string) => void }) { return <section className="mb-7 grid gap-5 lg:grid-cols-[1.25fr_0.75fr]"><div className="dashboard-card p-5 sm:p-6"><div className="flex items-center justify-between"><div><div className="wayfinding-label text-muted-foreground">SERVICE DESK</div><h2 className="mt-2 font-display text-lg font-bold">Integration health</h2></div><span className="flex items-center gap-2 text-xs font-bold text-emerald-600"><span className="h-2 w-2 rounded-full bg-emerald-500" /> Healthy</span></div><div className="mt-6 grid gap-3 sm:grid-cols-3">{["Booking API", "Payment webhooks", "Route sync"].map((name, i) => <button key={name} onClick={() => onNotify(`${name} diagnostics opened`)} className="rounded-xl border border-border/70 p-4 text-left transition hover:-translate-y-0.5 hover:border-[#F26A3D]/50"><div className="flex items-center justify-between"><span className="text-xs font-bold">{name}</span><Check size={15} className="text-emerald-500" /></div><div className="mt-3 font-display text-xl font-bold">{i === 0 ? "99.96%" : i === 1 ? "240ms" : "2m ago"}</div><div className="mt-1 text-[10px] text-muted-foreground">Click for diagnostics</div></button>)}</div></div><div className="dashboard-card p-5 sm:p-6"><div className="wayfinding-label text-muted-foreground">INCIDENT QUEUE</div><h2 className="mt-2 font-display text-lg font-bold">{seedIncidents.length} open items</h2><div className="mt-5 space-y-3">{seedIncidents.map((item) => <button key={item.id} onClick={() => onNotify(`${item.title} details opened`)} className="w-full rounded-xl bg-muted/45 p-3 text-left"><div className="flex justify-between gap-3 text-xs font-bold"><span>{item.title}</span><span className="text-[#F26A3D]">{item.severity}</span></div><div className="mt-1 text-[11px] text-muted-foreground">{item.updated} · {item.owner}</div></button>)}</div></div></section>; }

function ClientPanel({ language, onBook }: { language: Language; onBook: () => void }) { return <section className="mb-7 grid gap-5 lg:grid-cols-[1.25fr_0.75fr]"><div className="dashboard-card relative overflow-hidden bg-[#183B56] p-6 text-white"><div className="absolute inset-0 opacity-25" style={{ backgroundImage: "url('/manus-storage/boarding-pass-detail_5efae202.jpg')", backgroundPosition: "center", backgroundSize: "cover" }} /><div className="relative z-10"><div className="flex items-center justify-between"><div><div className="wayfinding-label text-white/55">CLIENT DESK</div><h2 className="mt-2 font-display text-2xl font-bold">Plan your next movement.</h2></div><Ticket className="text-[#F26A3D]" size={25} /></div><p className="mt-3 max-w-md text-sm leading-6 text-white/70">Create a booking, keep your passenger list current, and review every ticket from one ledger.</p><Button className="mt-6 rounded-xl bg-[#F26A3D] text-white" onClick={onBook}>Create a booking <ArrowRight size={16} className="ml-2" /></Button></div></div><div className="dashboard-card p-5 sm:p-6"><div className="wayfinding-label text-muted-foreground">AVAILABLE ROUTES</div><h2 className="mt-2 font-display text-lg font-bold">{seedRoutes.length} scheduled routes</h2><div className="mt-5 space-y-3">{seedRoutes.map((route) => <div key={route.code} className="flex items-center justify-between border-b border-dashed border-border pb-3 last:border-0"><div><div className="text-xs font-bold">{route.route}</div><div className="mt-1 text-[11px] text-muted-foreground">{route.code} · Gate {route.gate}</div></div><span className="text-xs font-bold text-emerald-600">{route.load}</span></div>)}</div></div></section>; }

function BookingsPanel({ bookings, onBook, onNotify }: { bookings: DemoBooking[]; onBook: () => void; onNotify: (message: string) => void }) { return <section className="dashboard-card overflow-hidden"><div className="flex flex-col gap-4 border-b border-border/70 p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><div><div className="wayfinding-label text-[#F26A3D]">BOOKING DESK</div><h2 className="mt-2 font-display text-2xl font-bold">All bookings</h2><p className="mt-1 text-sm text-muted-foreground">Review and create bookings without leaving the workspace.</p></div><Button className="rounded-xl bg-[#F26A3D] text-white" onClick={onBook}><Plus size={15} className="mr-2" /> New booking</Button></div><div className="flex items-center justify-between border-b border-border/70 bg-muted/25 px-5 py-3 text-xs text-muted-foreground sm:px-6"><span>{bookings.length} visible booking records</span><button onClick={() => onNotify("Booking filters opened")} className="font-bold text-[#F26A3D]">Filter manifest</button></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left"><thead className="text-[10px] uppercase tracking-[0.12em] text-muted-foreground"><tr><th className="px-5 py-3 sm:px-6">ID</th><th className="px-3 py-3">Passenger</th><th className="px-3 py-3">Route</th><th className="px-3 py-3">Departure</th><th className="px-3 py-3">Status</th><th className="px-5 py-3 text-right sm:px-6">Total</th></tr></thead><tbody className="divide-y divide-border/70">{bookings.map((booking) => <tr key={booking.id} className="hover:bg-muted/30"><td className="px-5 py-4 font-mono text-xs sm:px-6">{booking.id}</td><td className="px-3 py-4 text-xs font-bold">{booking.passenger}</td><td className="px-3 py-4"><span className="route-chip"><span className="waypoint-dot waypoint-dot--small" />{booking.route}</span></td><td className="px-3 py-4 text-xs text-muted-foreground">{booking.departure}</td><td className="px-3 py-4"><Badge className="rounded-full bg-emerald-100 text-[10px] text-emerald-700">{booking.status}</Badge></td><td className="px-5 py-4 text-right text-xs font-bold sm:px-6">{booking.total}</td></tr>)}</tbody></table></div></section>; }

function RoutesPanel({ onAdd, onNotify }: { onAdd: () => void; onNotify: (message: string) => void }) { return <section className="grid gap-5 lg:grid-cols-[1.2fr_0.8fr]"><div className="dashboard-card p-5 sm:p-6"><div className="flex items-center justify-between"><div><div className="wayfinding-label text-[#F26A3D]">NETWORK MAP</div><h2 className="mt-2 font-display text-2xl font-bold">Routes & schedules</h2><p className="mt-1 text-sm text-muted-foreground">Manage the movements your clients can book.</p></div><Button className="rounded-xl bg-[#F26A3D] text-white" onClick={onAdd}><Plus size={15} className="mr-2" /> Add route</Button></div><div className="mt-6 space-y-3">{seedRoutes.map((route) => <div key={route.code} className="flex flex-col gap-3 rounded-2xl border border-border/70 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#F26A3D]/10 text-[#F26A3D]"><Plane size={17} /></span><div><div className="font-display font-bold">{route.route}</div><div className="mt-1 text-xs text-muted-foreground">{route.code} · Gate {route.gate}</div></div></div><div className="flex items-center gap-3"><span className="text-xs font-bold text-emerald-600">{route.health}</span><span className="text-xs text-muted-foreground">{route.load} load</span><button onClick={() => onNotify(`${route.code} schedule opened`)} className="rounded-lg p-2 text-muted-foreground hover:bg-accent"><ArrowUpRight size={15} /></button></div></div>)}</div></div><div className="dashboard-card overflow-hidden bg-[#183B56] p-5 text-white sm:p-6"><div className="wayfinding-label text-white/55">SCHEDULE PULSE</div><h2 className="mt-2 font-display text-2xl font-bold">38 flights today</h2><div className="mt-8 h-2 overflow-hidden rounded-full bg-white/15"><div className="h-full w-[78%] rounded-full bg-[#F26A3D]" /></div><div className="mt-3 flex justify-between text-xs text-white/60"><span>Seats occupied</span><span>78%</span></div><div className="mt-8 border-t border-dashed border-white/20 pt-4 text-xs text-white/65">Next departure: ET 602 · ADD → DXB · 06:40</div></div></section>; }

function CustomersPanel({ users, onAdd, onNotify }: { users: DemoUser[]; onAdd: () => void; onNotify: (message: string) => void }) { return <section className="dashboard-card overflow-hidden"><div className="flex flex-col gap-4 border-b border-border/70 p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><div><div className="wayfinding-label text-[#F26A3D]">PEOPLE DIRECTORY</div><h2 className="mt-2 font-display text-2xl font-bold">Customers & users</h2><p className="mt-1 text-sm text-muted-foreground">Review the people who can access Mila workspaces.</p></div><Button className="rounded-xl bg-[#F26A3D] text-white" onClick={onAdd}><Plus size={15} className="mr-2" /> Add user</Button></div><div className="grid gap-3 p-5 sm:grid-cols-2 sm:p-6">{users.map((user) => <button key={user.id} onClick={() => onNotify(`${user.name} profile opened`)} className="flex items-center justify-between rounded-2xl border border-border/70 p-4 text-left transition hover:-translate-y-0.5 hover:border-[#F26A3D]/50"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-[#183B56] text-xs font-bold text-white">{user.initials}</span><div><div className="text-sm font-bold">{user.name}</div><div className="mt-1 text-xs text-muted-foreground">{user.email}</div></div></div><Badge variant="outline" className={`rounded-full border-0 text-[10px] ${user.status === "Active" ? "bg-emerald-100 text-emerald-700" : "bg-amber-100 text-amber-700"}`}>{user.status}</Badge></button>)}</div></section>; }

function SupportPanel({ onNotify }: { onNotify: (message: string) => void }) { return <section className="grid gap-5 lg:grid-cols-[1fr_1fr]"><div className="dashboard-card p-5 sm:p-6"><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-[#F26A3D]/10 text-[#F26A3D]"><Headphones size={20} /></span><div><div className="wayfinding-label text-[#F26A3D]">SERVICE DESK</div><h2 className="mt-2 font-display text-2xl font-bold">Support Center</h2></div></div><p className="mt-5 max-w-lg text-sm leading-6 text-muted-foreground">Get help with bookings, integrations, route setup, and client access. Every request gets a clear owner and next movement.</p><div className="mt-6 flex flex-wrap gap-2"><Button className="rounded-xl bg-[#F26A3D] text-white" onClick={() => onNotify("New support request opened")}>New support request <ArrowRight size={15} className="ml-2" /></Button><Button variant="outline" className="rounded-xl" onClick={() => onNotify("Help center opened")}>Browse help center</Button></div></div><div className="dashboard-card p-5 sm:p-6"><div className="flex items-center justify-between"><div><div className="wayfinding-label text-muted-foreground">OPEN QUEUE</div><h2 className="mt-2 font-display text-xl font-bold">2 active items</h2></div><span className="flex items-center gap-2 text-xs font-bold text-emerald-600"><span className="h-2 w-2 rounded-full bg-emerald-500" /> Healthy</span></div><div className="mt-5 space-y-3">{seedIncidents.map((incident) => <button key={incident.id} onClick={() => onNotify(`${incident.title} details opened`)} className="w-full rounded-xl bg-muted/45 p-3 text-left"><div className="flex items-center justify-between gap-3 text-xs font-bold"><span>{incident.title}</span><span className="text-[#F26A3D]">{incident.severity}</span></div><div className="mt-1 text-[11px] text-muted-foreground">{incident.owner} · {incident.updated}</div></button>)}</div></div></section>; }

function Dialog({ title, onClose, children }: { title: string; onClose: () => void; children: ReactNode }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#10283a]/45 p-4 backdrop-blur-sm"><div className="relative w-full max-w-[480px] rounded-[24px] border border-border bg-card p-6 shadow-[0_24px_70px_rgba(16,40,58,0.26)] sm:p-8"><button onClick={onClose} className="absolute right-4 top-4 rounded-lg p-2 text-muted-foreground hover:bg-accent"><X size={18} /></button><div className="mb-6"><div className="wayfinding-label text-[#F26A3D]">MILA WORKSPACE</div><h2 className="mt-2 font-display text-xl font-bold">{title}</h2></div>{children}</div></div>; }
function Field({ label, value, onChange, placeholder, type = "text" }: { label: string; value: string; onChange: (value: string) => void; placeholder: string; type?: string }) { return <label className="block text-xs font-bold">{label}<Input type={type} value={value} onChange={(e) => onChange(e.target.value)} placeholder={placeholder} className="mt-2 h-10 rounded-xl bg-background text-sm" /></label>; }
function Brand({ onClick }: { onClick: () => void }) { return <button onClick={onClick} className="flex items-center gap-3 text-left"><span className="brand-mark flex h-10 w-10 items-center justify-center rounded-[14px] bg-[#F26A3D] text-white shadow-[0_10px_24px_rgba(242,106,61,0.25)]"><Plane size={18} className="rotate-90" /></span><span><span className="block font-display text-[16px] font-bold tracking-[-0.03em]">Mila</span><span className="wayfinding-label text-muted-foreground">AIRLINE CMS</span></span></button>; }
function FeatureStat({ number, label }: { number: string; label: string }) { return <div className="pr-4"><div className="font-display text-2xl font-bold tracking-[-0.05em]">{number}</div><div className="mt-1 text-[11px] leading-4 text-muted-foreground">{label}</div></div>; }
function StatCard({ label, value, change, icon: Icon, negative = false }: { label: string; value: string; change: string; icon: typeof Activity; negative?: boolean }) { return <div className="dashboard-card relative overflow-hidden p-5 transition duration-200 hover:-translate-y-0.5"><span className="ticket-notch ticket-notch--left" /><span className="ticket-notch ticket-notch--right" /><div className="flex items-start justify-between"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#F26A3D]/10 text-[#F26A3D]"><Icon size={17} /></div><span className={`flex items-center gap-0.5 text-[11px] font-bold ${negative ? "text-rose-500" : "text-emerald-600"}`}><ArrowUpRight size={13} />{change}</span></div><div className="mt-5"><div className="wayfinding-label text-muted-foreground">{label}</div><div className="mt-1 font-display text-[26px] font-bold tracking-[-0.05em]">{value}</div><div className="mt-1 text-[11px] text-muted-foreground">vs last month</div></div></div>; }
function HealthRow({ label, value, percent, color }: { label: string; value: string; percent: number; color: "orange" | "blue" | "green" }) { const colors = { orange: "bg-[#F26A3D]", blue: "bg-[#183B56]", green: "bg-emerald-500" }; return <div><div className="mb-2 flex justify-between text-xs"><span className="font-semibold">{label}</span><span className="font-mono text-muted-foreground">{value}</span></div><div className="h-1.5 overflow-hidden rounded-full bg-muted"><div className={`h-full rounded-full ${colors[color]}`} style={{ width: `${percent}%` }} /></div></div>; }
function GovernanceRow({ label, value }: { label: string; value: string }) { return <div className="flex items-center justify-between border-b border-dashed border-border pb-3 text-xs last:border-0"><span className="text-muted-foreground">{label}</span><span className="font-display text-lg font-bold">{value}</span></div>; }
